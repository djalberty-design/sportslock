# Grok implementation plan — Friday, September 18, 2026

Working plan after DJ's answers. Execute **one phase at a time**. Push each phase to `main`. Morning pull time: **6:05 AM ET** (`5 10 * * *` UTC).

## Locked decisions

- Guest desk stays open.
- Default wager **$10**.
- Screen lines = morning cache. User edits price in the lock pop-out.
- Cross-sport parlays allowed, with filters in Phase B.
- Props stay off on the Feed. Admin Fetch Props remains (1 Odds API request per event).
- One Odds API mains pull per day at 6:05 AM ET. One ESPN **web-host** scoreboard probe per sport. No bulk ESPN props.
- Live scores: MLB StatsAPI, NHL web API, ESPN `site.web.api.espn.com`. No Odds API `/scores`.
- Live inning / quarter: **confirmed on production** (DJ, 2026-09-18 evening).
- `site.api.espn.com` is dead / 403. Do not wait on the block. Duration unknown.

## HOLD (2026-09-18 evening — do not build)

DJ: live inning worked. Hold player-prop expansion. Dig later.

| Idea | Status | Why held |
|---|---|
| Players without lines (StatsAPI / NHL roster + last-10) → `buildPropChance` on generic overs | **HOLD** | Ranks stacks. Cannot price vs Hard Rock. Looks like fake Feed props. |
| Auto top-3 featured games → Odds API event props | **HOLD** | Each game is a credit. Quota is ~500/mo. Fetch Props on one ticket is enough. |
| “Prop lines without players” | **Dead** | Odds API event endpoint returns player + line + price together. |
| Feed player props | **Off** | Unchanged. |

Reopen only on explicit go. Smallest later slice: MLB-only research cards, stamp `research`, Feed stays dark.

Featured-3 rubric (if ever built): skip live/final/no-id/weak-prop sports; score open ticket + scan EV + ticket/handle split + predict volume + injuries + start window; cap 3 auto pulls.

## Phase A — Honest prices, $10 default, one-tap straight (shipped)

## Phase B — Feed filters and logos (shipped)

## Phase C — Lock It In → My Action (shipped)

Write paperTickets + prediction_logs. Open chip counts opens. `/desk` → `/ticket`. Personal book on My Action.

## Phase D — Daily pulls at 6:05 AM ET (shipped)

Odds API mains once (regular season / playoffs only). 26h cache so page views do not buy requests. ESPN web-host scoreboard probed once per sport; 403 stays Looked. Admin Fetch Props on game header if needed.

Cron: `5 10 * * *` UTC = 6:05 AM ET. Grade stays on `0 10 * * *`. Stamp key: `odds_api_cache.morning-pull`.

## Phase E — Live games (shipped, confirmed)

LIVE badge + score + **Top/Bot inning** / quarter / period. MLB StatsAPI, NHL web API, ESPN web host. 2-minute cache. No Odds API `/scores`. No Feed player props.

## Phase F — Chrome and ET time (shipping with this hold doc)

Replace “Live Feed Synced: Just now” / “AI Engine: Optimal” / “v4.2.0” with an Eastern desk stamp (`formatKickoff`, compact). Florida desk stays on `America/New_York`.

## Phase G — Verify morning-pull + leftover law

Confirm the 6:05 AM job writes `morning-pull`. Confirm NFL/NBA/college LIVE after that stamp. Google OAuth still broken (out of scope unless asked).
