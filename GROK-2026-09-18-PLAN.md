# Grok implementation plan — Friday, September 18, 2026

Working plan after DJ's answers. Execute **one phase at a time**. Push each phase to `main`. Morning pull time: **6:00 AM ET** (confirmed).

## Locked decisions

- Guest desk stays open.
- Default wager **$10**.
- Screen lines = morning cache. User edits price in the lock pop-out.
- Cross-sport parlays allowed, with filters in Phase B.
- Props-off overridden once pulls and pricing work (Phase D).
- One Odds API mains pull per day. One ESPN player-props pull per day, 6 AM ET. Admin Odds API prop button remains if ESPN is dead.
- Live scores: free league APIs (MLB / NHL / NBA official-ish). ESPN scoreboard only if the morning ESPN call returns 200. No Odds API `/scores` unless quota is explicitly spent later.

## Phase A — Honest prices, $10 default, one-tap straight (shipped)

1. Game ticket American = book price from Odds API / Hard Rock field. No +100 fallback.
2. Displayed chance = model only in 8–90%. Otherwise book implied or Looked. No 98% both sides.
3. First tap = straight. Second tap adds a leg.
4. Lock modal Final Odds = combined book American from selected legs. Reject |odds| > 10000.
5. Default wager $10. Feed payout uses the same American.
6. Feed HIT PROB = calibrated `pick.chance`, not raw combinedFair (97% bar vs 86% copy).
7. Matchups projection = scan chance or book no-vig. Remove the hash demo bar.

## Phase B — Feed filters and logos (shipped)

1. Sport chips stay. **SOON** only when that sport is missing from snapshot quotes/briefs — not from an empty ribbon.
2. Mix chips: All mixes / Same game / Same sport / Cross sport / 2-leg / 3-leg.
3. Filter the full ribbon, then show up to 12 cards. A sport with games but no ribbon is empty Feed, not a fake SOON board.
4. Each Feed card names the mix. Each leg resolves a team mark from quote logo, ESPN abbr, or full team name.
5. Season note under an empty filtered Feed.

## Phase C — Lock It In → My Action (this push)

Write paperTickets + prediction_logs. Open chip counts opens. `/desk` → `/ticket`. Personal book on My Action.

## Phase D — Daily pulls at 6 AM ET

Odds API mains once (regular season / playoffs only). 26h cache so page views do not buy requests. ESPN player props once. Admin Fetch Props on game header if ESPN is down.

Cron target: `0 10 * * *` UTC = 6:00 AM ET. Grade already sits on that slot; the morning pull is a new path, not a hijack of grade.

## Phase E — Live games

LIVE badge + period + score from free APIs / ESPN-if-up.

## Phase F — Chrome and local time

Local date+time stamp. Drop Just now / Optimal / v4.2.0.

## Phase G — Verify + current-law doc
